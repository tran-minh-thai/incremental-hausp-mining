# Python dependencies for the analysis scripts (analysis/*.py).
# Install with:  python3 -m pip install -r analysis/requirements.txt
# Verified working on Python 3.9.6 (macOS Command Line Tools).
pandas==2.3.3
matplotlib==3.9.4
numpy==2.0.2
tabulate==0.9.0
scipy==1.13.1
